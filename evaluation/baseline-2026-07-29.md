# 기준점 — 2026-07-29

스킬 보강과 슬림화에 들어가기 직전의 값이다.
이후 변경의 효과를 이 값과 비교해 가른다.

## 기계 축

`evaluation/score.sh` 실측이다.

| 스킬 | 본문줄 | 참조수 | 끊긴참조 | 표기위반 |
|---|---:|---:|---:|---:|
| planning | 199 | 9 | 0 | 0 |
| build-with-teams | 464 | 3 | 0 | 0 |
| review-fix | 366 | 0 | 0 | 0 |
| docs-check | 224 | 1 | 0 | 0 |

## 앞 기준점(2026-07-27) 대비 변화

| 스킬 | 본문줄 변화 | 원인 |
|---|---|---|
| planning | 190 → 199 | 통과 조건을 모호점 0 으로 바꾼 변경과 `version` 한 줄 |
| build-with-teams | 458 → 464 | 스폰 폴백 단계화, 등급 상속 정의, `version` 한 줄 |
| review-fix | 365 → 366 | `version` 한 줄 |
| docs-check | 223 → 224 | `version` 한 줄 |

`review-fix` 와 `docs-check` 의 변화량이 `version` 한 줄과 정확히 일치한다.
버전 체계 도입이 지시 내용을 바꾸지 않았다는 확인이다.

## 판정 축은 이번에 재지 않았다

`rubric.md` 의 정성 축(지침 정렬, 강제력, 계층 분리)은 독립 에이전트가 매겨야 한다.
이번 측정에서는 실행하지 않았으므로 이 파일에는 기계 축만 있다.
정성 축이 필요한 시점은 슬림화 직후다. 지시를 덜어냈을 때 강제력이 떨어졌는지는 기계 축으로 안 보인다.

## 다음 변경 전에 알아 둘 것

- `build-with-teams` 464줄은 권고 500 에 가깝다. 지시를 더 넣으려면 `references/` 로 빼야 한다.
- `review-fix` 는 366줄인데 참조가 0 이다. 계층 분리가 가장 덜 된 스킬이라 슬림화 효과가 클 후보다.
- 앞 기준점에서 지적된 "끊긴참조·표기위반 검사가 만들어진 직후라 실효를 아직 모른다" 는 조건이 이틀 뒤인 지금도 유효하다.
