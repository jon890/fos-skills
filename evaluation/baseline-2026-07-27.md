# 기준점 — 2026-07-27

`evaluation/rubric.md` 기준 첫 측정이다.
이 시점 직전에 스킬 4종을 대폭 손봤으므로, 개선 전 값은 남아 있지 않다.
이 파일은 **개선 후 상태**이며 앞으로의 비교 기준이 된다.

## 기계 축

`evaluation/score.sh` 실측이다.

| 스킬 | 본문줄 | 참조수 | 끊긴참조 | 표기위반 |
|---|---:|---:|---:|---:|
| planning | 190 | 9 | 0 | 0 |
| build-with-teams | 458 | 3 | 0 | 0 |
| review-fix | 365 | 0 | 0 | 0 |
| docs-check | 223 | 1 | 0 | 0 |

읽을 점.

- 끊긴참조·표기위반은 전부 0 이다. 다만 이 두 검사가 만들어진 것이 오늘이라 **아직 시간이 지난 뒤의 값이 아니다.** 검사의 실효는 몇 주 뒤 다시 재 봐야 안다.
- `build-with-teams` 458줄은 권고 500 에 가깝다. 더 늘리려면 references 로 빼야 한다.
- `review-fix` 는 365줄인데 references 가 0 이다. 계층 분리가 가장 덜 된 스킬이다.

측정 도구를 만들자마자 실제로 결함을 하나 잡았다.
`planning` 과 `review-fix` 가 `references/run-record.md` 를 자기 번들 경로처럼 적었는데 그 파일은 `build-with-teams` 에 있었다.
`verify-task.sh` 경로 오류와 같은 계열이며, 이번에는 문서를 쓴 직후 바로 드러났다.

## 판단 축

**미측정.** 채점표가 작성자 자신이 매기지 말라고 정하고 있고, 이번 세션이 곧 작성 세션이다.
같은 맥락에서 매기면 후하게 나오므로 비워 둔다.

| 스킬 | A 지침정렬 | B 강제력 | C 참조무결성 | D 계층분리 |
|---|:---:|:---:|:---:|:---:|
| planning | — | — | — | — |
| build-with-teams | — | — | — | — |
| review-fix | — | — | — | — |
| docs-check | — | — | — | — |

측정하려면 별도 세션에서 `evaluation/rubric.md` 와 대상 스킬을 독립 검토 에이전트에 넘긴다.

## 이 시점에 알려진 미해결 항목

측정과 별개로, 검토 중 확인했으나 아직 손대지 않은 것들이다.

- `planning` 에 위임 캡이 없다. 다른 세 스킬에는 있다.
- 파일 산출물 분량 보정이 `step-7` 의 ADR 항목에만 있다. task·phase·PR 본문·회고에는 없다.
- `review-fix` 가 361줄인데 references 로 분리된 것이 없다.

## 다음 측정 때 볼 것

- 끊긴참조가 계속 0 인가. 새 참조를 추가하며 다시 깨뜨리지 않았는가.
- 실행 기록(`docs/retrospectives/RUNS.md`)에 쌓인 값의 추세. 특히 사용자 개입 횟수.
- 반복 관측되던 결함 유형 — 검사 없이 조용히 누적되는 규칙 — 이 다시 나오는가.
