# 검사기 매핑 표 유지보수

`판` 과 `정점` 은 금지어로 등록할 수 없다.
검사기가 부분 문자열로 찾아 `판단`·`판정` 과 `그래프의 정점`·`수정점검` 까지 걸린다.

`재다` 도 활용형을 가려 등록했다. 매핑 표에는 `잰`·`쟀`·`재어` 만 있다.
아래 넷은 같은 이유로 뺐다. 다시 넣으면 오탐이 난다.

| 뺀 활용형 | 함께 걸리는 말 |
| --- | --- |
| `재는` | 현재는, 존재는, 소재는 |
| `재고` | 재고(inventory) |
| `재면` | 현재면, 부재면 |
| `재야` | 재야(在野) |

등록한 셋으로는 `재색인`·`재현율`·`재시도`·`재고` 가 걸리지 않는 것을 확인했다.
