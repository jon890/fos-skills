---
name: pr-review
description: |
  남의 PR 에 코드 리뷰를 작성하고 등록하는 공용 코어 스킬. 문제를 찾는 일이 아니라,
  찾은 것을 근거와 등급을 갖춰 쓰고 미리보기로 확인한 뒤 PR 에 등록하는 절차를 소유한다.
  "/pr-review", "pr-review", "PR 리뷰해줘", "코드 리뷰 해줘", "리뷰 작성", "리뷰 등록",
  "리뷰 남겨줘", "이 PR 봐줘", "리뷰 초안", "리뷰 댓글 달아줘" 같은 표현이면 이 스킬을 쓴다.
  이미 달린 리뷰를 코드에 반영하는 일은 `review-fix` 가 맡는다. 방향이 반대다.
  레포별 특화(호스트, 등급 표기, 후속 업무 프로젝트)는 레포 오버레이로 주입된다.
metadata:
  version: "1.0.0"
---

# pr-review

남의 PR 에 리뷰를 남긴다. 이 스킬이 소유하는 것은 **작성과 등록**이다.

문제를 찾는 일은 다른 것이 한다. `/code-review` 내장 명령, 직접 읽기, 실행해 보기 중 무엇을 써도 된다.
이 스킬은 그렇게 찾은 것을 **틀리지 않게 쓰고 안전하게 올리는** 절차다.

## 어느 reference 를 읽을지

| 하려는 일 | 문서 |
| --- | --- |
| 주장이 맞는지 확인한다 | [evidence.md](references/evidence.md) |
| 등급을 매긴다 | [grading.md](references/grading.md) |
| 리뷰로 낼지 후속 업무로 낼지 가른다 | [scope.md](references/scope.md) |
| PR 에 등록한다 | [posting.md](references/posting.md) |

## 첫 단계: 레포 오버레이

`<repo-root>/.claude/pr-review-overlay.md` 가 있으면 먼저 읽고 코어보다 우선한다.

- 오버레이가 채우는 것: git 호스트, 등급 표기, 후속 업무를 등록할 곳, 저장소 고유 점검 항목.
- 없으면 레포 `CLAUDE.md` 와 `REVIEW.md` 를 본다. 셋 다 없으면 사용자에게 확인한다.
- 오버레이는 코어를 덮어쓰지 않고 채운다. 아래 다섯 단계는 생략할 수 없다.

**스킬 파일은 세션 중에 바뀔 수 있다.** 컨텍스트에 실린 사본은 세션 시작 시점에 고정되므로,
`content-preview` 나 `dooray-task` 를 실제로 부르기 직전에 그 `SKILL.md` 를 다시 읽는다.

## 다섯 단계

```
대상 확정 → 근거 확보 → 등급과 범위 판정 → 미리보기(턴 종료) → 등록과 검증
```

각 단계를 건너뛰면 되돌리는 비용이 크다. 잘못 등록한 리뷰는 작성자가 읽은 뒤에야 지울 수 있다.

### 1단계: 대상 확정

PR 번호와 호스트를 먼저 정한다. 호스트를 틀리면 조회가 전부 404 로 실패한다.

```bash
git remote -v                      # 호스트 확인. github.com 이 아니면 GH_HOST 가 필요하다
gh pr list --state open --limit 20
```

리뷰할 범위를 사용자와 맞춘다. 파일 하나인지, 특정 관심사인지, 전체인지.
전체 리뷰를 요청받지 않았으면 스스로 범위를 넓히지 않는다.

### 2단계: 근거 확보

**주장하기 전에 잰다.** 이 스킬에서 가장 값이 큰 규칙이다.

[evidence.md](references/evidence.md) 를 따른다. 요약하면 셋이다.

- 동작 주장은 코드 인용으로 받친다. 이름에서 추론한 것은 근거가 아니다.
- 성능·메모리·빈도 주장은 실측 없이 쓰지 않는다.
- 잴 수 없으면 등급을 내리거나 질문형으로 바꾼다.

### 3단계: 등급과 범위 판정

[grading.md](references/grading.md) 로 등급을, [scope.md](references/scope.md) 로 리뷰인지 후속 업무인지 가른다.

작성자가 이 PR 에서 고칠 수 있는 것만 리뷰로 낸다.
저장소 공통 문제를 리뷰에 섞으면 작성자가 감당할 수 없는 것을 요구하게 된다.

### 4단계: 미리보기

`content-preview` 스킬을 따른다. 그 스킬이 절차를 소유한다.

- 본문 전문을 채팅에 다시 쓰지 않는다. 위치와 등급, 담은 내용의 요약만 보고한다.
- 미리보기 턴은 미리보기로 끝낸다. 등록 확인은 사용자가 읽은 다음 턴에 받는다.
- 본인 명의로 나가는 글이므로 업무 글 페르소나를 **본문을 쓰기 전에** 읽는다.

### 5단계: 등록과 검증

[posting.md](references/posting.md) 를 따른다. 등록 뒤에는 반드시 확인한다.

```bash
scripts/gh-review-post.sh <owner/repo> <PR> <path> <line> <body.md>
```

이 스크립트는 등록 전에 본문을 검증한다. 검증 없이 `gh api` 를 직접 부르지 않는다.

## 반드시 지킬 것

- **틀린 주장을 등록하지 않는다.** 확신이 없으면 질문형으로 쓴다. 작성자가 되돌리는 비용이 내가 한 번 더 재는 비용보다 크다.
- **정정은 미루지 않는다.** 실측으로 내 주장이 뒤집히면 그 자리에서 답글로 밝힌다. 등급이 과했으면 과했다고 적는다.
- **작성자가 두 번 반론하면 멈춘다.** 세 번째 설득은 대개 논거가 아니라 고집이다. 결정은 작성자에게 남긴다.
- **한 리뷰에 한 논점만 담는다.** 여러 개를 묶으면 작성자가 하나만 답하고 나머지가 묻힌다.
- **칭찬이나 확인만 하는 댓글로 스레드를 늘리지 않는다.** 반영된 것은 resolve 로 표시한다.

## 하지 않는 것

- 서식과 취향을 기본 대상으로 삼지 않는다. CI 나 포매터가 잡는 것은 리뷰하지 않는다.
- 리뷰 안에서 코드를 고치지 않는다. 고치는 것은 `review-fix` 나 작성자의 일이다.
- 등급을 인상하는 방향으로 재조정하지 않는다. 실측이 약하면 내리기만 한다.
